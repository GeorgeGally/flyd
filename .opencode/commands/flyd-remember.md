---
description: Capture something to flyd memory — store all, decide later
argument-hint: "<text>"
---
Run `npx tsx src/index.ts $ARGUMENTS` to capture text to raw memory. The capture is auto-indexed and searchable immediately — no governance gate.

After running the command, confirm to the user what was captured and that it's available via `/flyd-ask`.
