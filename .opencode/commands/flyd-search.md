---
description: Search flyd memory — raw retrieval without synthesis
argument-hint: "<query>"
---
Run `npx tsx src/index.ts search $ARGUMENTS` for raw keyword/vector search across all raw captures. Returns top matching entries without LLM synthesis. No governance gate — everything is findable.
