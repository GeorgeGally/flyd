<%= form_with model: @project, class: "space-y-7 border-t border-ink pt-7" do |f| %>
  <% if @project.errors.any? %>
    <div class="sys-strip sys-strip--plain">
      <h3 class="text-sm font-medium text-ink"><%= pluralize(@project.errors.count, "error") %> prohibited this project from being saved:</h3>
      <ul class="mt-1 text-sm text-muted list-disc list-inside">
        <% @project.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.label :name, class: "flyd-system-label mb-1.5 block" %>
    <%= f.text_field :name, class: "flyd-system-input", placeholder: "Project name", autofocus: true %>
  </div>

  <div>
    <%= f.label :description, class: "flyd-system-label mb-1.5 block" %>
    <%= f.text_area :description, rows: 3, class: "flyd-system-input resize-none", placeholder: "What is this project about?" %>
  </div>

  <div>
    <%= f.label :root_path, class: "flyd-system-label mb-1.5 block" %>
    <%= f.text_field :root_path, class: "flyd-system-input font-mono", placeholder: "~/path/to/project (optional)" %>
    <p class="sys-composer__hint mt-2">Maps auto-captures from this directory to this project.</p>
  </div>

  <div class="sys-actions !mt-8">
    <%= f.submit class: "flyd-system-btn flyd-system-btn--primary cursor-pointer" %>
    <%= link_to "Cancel", @project.persisted? ? project_path(@project) : projects_path, class: "flyd-system-btn flyd-system-btn--ghost" %>
  </div>
<% end %>
